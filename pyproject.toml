[project]
name = "lightningboost"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []




[tool.uv.workspace]
members = [
    "backend",
    "cloud_backend",
    "frontend",
    "local_agent",
    "prototype",
    "shared"
]
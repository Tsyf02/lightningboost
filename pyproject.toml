[project]
name = "lightningboost"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
    "requests>=2.33.1",
    "uvicorn>=0.46.0",
]




[tool.uv.workspace]
members = [
    "backend",
    "cloud_backend",
    "frontend",
    "local_agent",
    "prototype",
    "shared"
]
